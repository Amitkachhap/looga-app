// components/ProductCard.js
const ProductCard = ({ product }) => (
    <div>
      <h3>{product.name}</h3>
      {/* Add more details or link to the product page */}
    </div>
  );
  
  export default ProductCard;
  